(() => {'use strict'; document.documentElement.classList.add('js'); (function(){if (window.__mfPopupGuard) return; window.__mfPopupGuard = true; var realOpen = window.open; window.open = function(url, name, features){try {console.warn('[mengflix] blocked pop-up to', url); if (window.__mfOnPopupBlocked) window.__mfOnPopupBlocked(url);} catch (e) {}return null;};})(); window.__mfOnPopupBlocked = function(target){if (!playerOverlay) return; var hint = document.getElementById('playerHint'); if (!hint) return; var original = hint.dataset.original || hint.innerHTML; if (!hint.dataset.original) hint.dataset.original = original; var msg = 'Pop-up blocked' + (target && typeof target === 'string' ? ' (' + target.slice(0, 60) + ')': ''); hint.innerHTML = '<strong style="color:#ff6b6b">' + msg + '</strong>. ' + original; clearTimeout(hint.__mfTimer); hint.__mfTimer = setTimeout(function(){hint.innerHTML = hint.dataset.original || original;}, 3500);}; var lastBlurAt = 0; window.addEventListener('blur', function(){lastBlurAt = Date.now(); setTimeout(function(){try {if (playerOverlay && !playerOverlay.hidden && Date.now() - lastBlurAt < 100){window.focus(); if (window.__mfOnPopupBlocked) window.__mfOnPopupBlocked('focus-steal attempt');}} catch (e) {}}, 60);}); var cardDataLoaded = false; var searchIndex = []; var cardReadyCallbacks = []; function onCardsReady(fn){if (cardDataLoaded) {fn();} else {cardReadyCallbacks.push(fn);}}function createCardElement(d, sectionId){var kindLabel = d.type === 'TVSeries' ? 'Series': d.type === 'Donghua' ? 'Donghua': 'Movie'; var art = document.createElement('article'); art.className = 'content-card'; art.setAttribute('itemscope', ''); art.setAttribute('itemtype', 'https://schema.org/' + (d.type === 'TVSeries' ? 'TVSeries': 'Movie')); art.setAttribute('data-title', d.title); art.setAttribute('data-type', d.type); art.setAttribute('data-poster', d.poster); art.setAttribute('data-year', String(d.year)); art.setAttribute('data-runtime', String(d.runtime)); art.setAttribute('data-genre', d.genre); art.innerHTML = '<a href="#" title="' + escAttr(d.title) + '" data-open>' +'<div class="card-poster-wrap">' +'<img src="' + escAttr(d.poster) + '" alt="' + escAttr(d.title) + '" loading="lazy" decoding="async" width="342" height="513">' +'<div class="card-play-overlay" aria-hidden="true"><div class="card-play-icon"><svg viewBox="0 0 24 24" aria-hidden="true"><polygon points="5 3 19 12 5 21 5 3"/></svg></div></div>' +'<div class="card-info-overlay"><h3 class="card-title">' + escapeHtml(d.title) + '</h3>' +'<div class="card-meta"><span>' + kindLabel + '</span><span class="sep">&middot;</span><span>' + d.year + '</span></div></div></div></a>'; return art;}function createDonghuaElement(key, d){var poster = d.poster || ''; var status = d.donghua_status || 'Ongoing'; var ep = d.latest_episode || ''; var year = d.year || ''; var rating = d.rating || d.stars || 0; var metaStr = status + (year ? ' (' + year + ')': '') + (ep ? ' EP' + ep: ''); var art = document.createElement('article'); art.className = 'content-card'; art.setAttribute('itemscope', ''); art.setAttribute('itemtype', 'https://schema.org/TVSeries'); art.setAttribute('data-title', d.title || key); art.setAttribute('data-type', 'Donghua'); art.setAttribute('data-poster', poster); art.setAttribute('data-year', year); art.setAttribute('data-rating', String(rating)); art.setAttribute('data-genre', d.genres || ''); art.setAttribute('data-status', status); art.innerHTML = '<a href="#" class="card-link" data-open data-title="' + escAttr(d.title || key) + '" data-type="Donghua" data-poster="' + escAttr(poster) + '">' +'<div class="card-poster">' +(poster ? '<img src="' + escAttr(poster) + '" alt="' + escAttr(d.title || key) + ' poster" loading="lazy" decoding="async" crossorigin="anonymous">': '') +'<div class="card-overlay"><span class="card-play-btn"><svg viewBox="0 0 24 24" aria-hidden="true"><polygon points="6 4 20 12 6 20 6 4"/></svg></span></div></div>' +'<div class="card-body"><h3 class="card-title" title="' + escAttr(d.title || key) + '">' + escapeHtml(d.title || key) + '</h3>' +'<p class="card-meta">' + escapeHtml(metaStr) + '<span class="card-meta-sep">|</span>' + (rating ? '&#9733;': '') + '</p></div></a>'; return art;}function renderCardsFromData(data){Object.keys(data).forEach(function(sectionId){var track = document.querySelector('#' + sectionId + ' .slider-track'); if (!track) return; data[sectionId].forEach(function(cardData){track.appendChild(createCardElement(cardData, sectionId));});});}function renderDonghuaFromData(data){var track = document.querySelector('#donghua .slider-track'); if (!track) return; Object.keys(data).forEach(function(key){try {var d = data[key]; if (d && typeof d === 'object') track.appendChild(createDonghuaElement(key, d));} catch(e){}});}async function loadAndRenderCards(){try {var res = await fetch('assets/cards.json?t=' + Date.now()); var data = await res.json(); renderCardsFromData(data);} catch(e){console.warn('[mf] cards.json load failed:', e);}try {var res2 = await fetch('assets/donghua.json?t=' + Date.now()); var data2 = await res2.json(); renderDonghuaFromData(data2);} catch(e){console.warn('[mf] donghua.json load failed:', e);}try {var res3 = await fetch('assets/search_index.json?t=' + Date.now()); searchIndex = await res3.json();} catch(e){console.warn('[mf] search_index.json load failed:', e);}cardDataLoaded = true; cardReadyCallbacks.forEach(function(fn){try{fn();}catch(e){}}); cardReadyCallbacks = [];}if (document.readyState === 'loading'){document.addEventListener('DOMContentLoaded', loadAndRenderCards);} else {loadAndRenderCards();}const slides = Array.from(document.querySelectorAll('.yf-hero-slide')); const dots = Array.from(document.querySelectorAll('.yf-hero-dot')); const prev = document.getElementById('heroPrev'); const next = document.getElementById('heroNext'); let idx = 0; let timer = null; function go(n){if (!slides.length) return; idx = (n + slides.length) % slides.length; slides.forEach((s, i) => s.classList.toggle('active', i === idx)); dots .forEach((d, i) => d.classList.toggle('active', i === idx));}function startAuto(){stopAuto(); timer = setInterval(() => go(idx + 1), 6500);}function stopAuto(){if (timer) {clearInterval(timer); timer = null;}}if (slides.length){prev && prev.addEventListener('click', () => {go(idx - 1); if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){startAuto();}}); next && next.addEventListener('click', () => {go(idx + 1); if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){startAuto();}}); dots.forEach(d => d.addEventListener('click', () => {const n = parseInt(d.dataset.go, 10); go(n); if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){startAuto();}})); const hero = document.querySelector('.yf-hero'); hero && hero.addEventListener('mouseenter', stopAuto); hero && hero.addEventListener('mouseleave', startAuto); document.addEventListener('keydown', (e) => {if (e.key === 'ArrowLeft') {go(idx - 1); if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){startAuto();}}if (e.key === 'ArrowRight') {go(idx + 1); if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){startAuto();}}}); if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){startAuto();}}const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)'); const sections = document.querySelectorAll('.section'); if ('IntersectionObserver' in window && sections.length){const io = new IntersectionObserver((entries) => {entries.forEach((e) => {if (e.isIntersecting){e.target.classList.add('is-visible'); io.unobserve(e.target);}});}, {rootMargin: '0px 0px -10% 0px', threshold: 0.05}); sections.forEach((s) => io.observe(s)); sections.forEach((s) => {const r = s.getBoundingClientRect(); if (r.top < window.innerHeight) s.classList.add('is-visible');});} else {sections.forEach((s) => s.classList.add('is-visible'));}document.querySelectorAll('.slider-wrap').forEach((wrap) => {const track = wrap.querySelector('.slider-track'); if (!track) return; if (!wrap.querySelector('.slider-progress')){const prog = document.createElement('div'); prog.className = 'slider-progress'; const bar = document.createElement('div'); bar.className = 'slider-progress-bar'; prog.appendChild(bar); wrap.appendChild(prog);}}); function setupSlider(wrap){const track = wrap.querySelector('.slider-track'); if (!track) return; const p = wrap.querySelector('.slider-btn-prev'); const n = wrap.querySelector('.slider-btn-next'); const bar = wrap.querySelector('.slider-progress-bar'); const step = () => Math.max(track.clientWidth * 0.85, 320); if (p){p.addEventListener('click', () => {track.scrollBy({left: -step(), behavior: prefersReducedMotion.matches ? 'auto': 'smooth'});});}if (n){n.addEventListener('click', () => {track.scrollBy({left: step(), behavior: prefersReducedMotion.matches ? 'auto': 'smooth'});});}function update(){const max = track.scrollWidth - track.clientWidth; const x = track.scrollLeft; wrap.classList.toggle('is-scrollable-left', x > 4); wrap.classList.toggle('is-scrollable-right', x < max - 4); if (p) p.disabled = x <= 1; if (n) n.disabled = x >= max - 1; if (bar && track.scrollWidth > track.clientWidth){const ratio = track.clientWidth / track.scrollWidth; const offset = (x / max) * (1 - ratio); bar.style.transform = 'translateX(' + (offset * 100) + '%) scaleX(' + ratio + ')';} else if (bar){bar.style.transform = 'translateX(0) scaleX(1)';}}track.addEventListener('scroll', update, {passive: true}); window.addEventListener('resize', update); update(); let isDown = false, startX = 0, startY = 0, startScroll = 0, dragged = false, activePointerId = null; function onDown(e){if (e.button !== undefined && e.button !== 0) return; isDown = true; dragged = false; startX = e.clientX; startY = e.clientY; startScroll = track.scrollLeft; activePointerId = (e.pointerId !== undefined ? e.pointerId: null); try {console.log('[dbg] down btn=', e.button, 'id=', 